const MainLink = ({ children }) => {
  return (
    <a className='inline-block bg-slate-50 text-slate-950 py-2 px-4 cursor-pointer rounded-lg hover:scale-120 transition duration-75'>
        {children}
    </a>

  )
}

export default MainLink